# Signed artifacts

Minimum artifacts to sign:
- run_manifest (config_hash, seed, datasets)
- summary metrics (with CI/p-value where applicable)
- logs (optional, but hash them)

Store:
- `artifacts/` with immutable naming and checksums.
