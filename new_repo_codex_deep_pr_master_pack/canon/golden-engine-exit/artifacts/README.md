# Artifacts

Store immutable run artifacts used for reproduction:
- run_manifest.yaml / json
- summary metrics + CI
- checksums + signature files

Use naming: `{run_ts}_{experiment_id}_{config_hash}/...`
