name: PR-001 Security Gates

on:
  pull_request:
  push:
    branches: ["**"]

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run PR-001 security guard
        run: bash scripts/security/pr001_guard.sh
